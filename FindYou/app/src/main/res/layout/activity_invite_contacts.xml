<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="group.csm117.findyou.InviteContactsActivity">


    <ImageView
        android:id="@+id/invite_contacts_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/invite_contacts"
        android:layout_alignParentTop="true"
        android:layout_marginTop="-50dp"
        android:layout_marginBottom="-30dp"
        android:contentDescription="@string/invite_contacts_contentDescription"
        android:cropToPadding="false" />


        <ListView
            android:id="@+id/listview"
            android:scrollbars="vertical"
            android:layout_width="fill_parent"
            android:layout_height="200dip"
            android:layout_gravity="fill_vertical"
            android:layout_below="@+id/invite_contacts_title"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_marginTop="27dp"
            android:layout_alignParentBottom="true">
        </ListView>

</RelativeLayout>
